<form method="post" action="{{ path('app_periodicite_delete', {'id': periodicite.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ periodicite.id) }}">
    <button class="btn">Supprimer</button>
</form>
