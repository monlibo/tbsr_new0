{% extends 'base.html.twig' %}

{% block title %}Periodicite{% endblock %}

{% block body %}
    <h1>Details Periodicite</h1>

    {% for message in app.flashes('success') %}
        <div class="alert alert-success"  style=" color: #008000" >
            {{ message }}
        </div>
    {% endfor %}

    <table class="table">
        <tbody>
            <tr>
                <th>Libelle</th>
                <td>{{ periodicite.libelle }}</td>
            </tr>
            <tr>
                <th>DateDebut</th>
                <td>{{ periodicite.dateDebut ? periodicite.dateDebut|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>DateFin</th>
                <td>{{ periodicite.dateFin ? periodicite.dateFin|date('Y-m-d') : '' }}</td>
            </tr>
{#            <tr>#}
{#                <th>CreatedAt</th>#}
{#                <td>{{ periodicite.createdAt ? periodicite.createdAt|date('Y-m-d H:i:s') : '' }}</td>#}
{#            </tr>#}
{#            <tr>#}
{#                <th>CreatedBy</th>#}
{#                <td>{{ periodicite.createdBy }}</td>#}
{#            </tr>#}
{#            <tr>#}
{#                <th>UpdatedAt</th>#}
{#                <td>{{ periodicite.updatedAt ? periodicite.updatedAt|date('Y-m-d H:i:s') : '' }}</td>#}
{#            </tr>#}
{#            <tr>#}
{#                <th>UpdatedBy</th>#}
{#                <td>{{ periodicite.updatedBy }}</td>#}
{#            </tr>#}
{#            <tr>#}
{#                <th>DeletedAt</th>#}
{#                <td>{{ periodicite.deletedAt ? periodicite.deletedAt|date('Y-m-d H:i:s') : '' }}</td>#}
{#            </tr>#}
{#            <tr>#}
{#                <th>DeletedBy</th>#}
{#                <td>{{ periodicite.deletedBy }}</td>#}
{#            </tr>#}
        </tbody>
    </table>

    <a href="{{ path('app_periodicite_index') }}">Retour à la liste</a>

    <a href="{{ path('app_periodicite_edit', {'id': periodicite.id}) }}">Modifier</a>

    {{ include('periodicite/_delete_form.html.twig') }}
{% endblock %}
