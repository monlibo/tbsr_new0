{% extends 'base.html.twig' %}

{% block title %}New Structure
{% endblock %}

{% block body %}
	{% block submenu %}Structure
	{% endblock %}

	<div class="card">
		<div class="card-body">
			<h4 class="card-title mb-3 pb-3 border-bottom">Enregistrer une Structure</h4>
			{% for message in app.flashes('error') %}
				<div class="alert alert-danger" style=" color: #B0413E">
					{{ message }}
				</div>
			{% endfor %}

			{{ include('structure/_form.html.twig') }}

		</div>
	</div>
	<a href="{{ path('app_structure_index') }}" class="btn btn-primary">Retour à la liste</a>

{% endblock %}
