{% extends 'base.html.twig' %}

{% block title %}Edit Structure
{% endblock %}

{% block body %}
	<div class="card">
		<div class="card-body">
			<h1>Modifier une Structure</h1>

			{{ include('structure/_form.html.twig', {'button_label': 'Modifier'}) }}

			<a href="{{ path('app_structure_index') }}">Retour à la liste</a>

			{{ include('structure/_delete_form.html.twig') }}
		</div>
	</div>
{% endblock %}
