<form method="post" action="{{ path('app_categorie_document_delete', {'id': categorieDocument.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorieDocument.id) }}">
    <button class="btn">Supprimer</button>
</form>
