<form method="post" action="{{ path('app_fonction_delete', {'id': fonction.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fonction.id) }}">
    <button class="btn btn-delete">Supprimer</button>
</form>
