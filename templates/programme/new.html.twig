{% extends 'base.html.twig' %}

{% block title %}programme{% endblock %}

{% block body %}
    <h1>Enregistrer un Programme</h1>

    {% for message in app.flashes('error') %}
        <div class="alert alert-danger"  style="background-color: #B0413E; color: #dbeafe" >
            {{ message }}
        </div>
    {% endfor %}

    {{ include('programme/_form.html.twig') }}

    <a href="{{ path('app_programme_index') }}">Retour à la liste</a>
{% endblock %}
