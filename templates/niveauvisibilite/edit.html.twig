{% extends 'base.html.twig' %}

{% block title %}Edit Reforme
{% endblock %}

{% block body %}
	{% block submenu %}Niveau de visibilité
	{% endblock %}

	<div class="card">
		<div class="card-body">
			<h4 class="card-title mb-3 pb-3 border-bottom">Modifier un niveau de visibilité</h4>
			{% for message in app.flashes('error') %}
				<div class="alert alert-danger" style=" color: #B0413E">
					{{ message }}
				</div>
			{% endfor %}

			{{ include('niveauvisibilite/_form.html.twig', {'button_label': 'Modifier'}) }}

		</div>
	</div>


	{# <h1>Enregistrer une Reforme</h1>
			
				{{ include('reforme/_form.html.twig') }} #}

	<a href="{{ path('app_reforme_index') }}">Retour à la liste</a>
{% endblock %}
