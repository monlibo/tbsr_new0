<form method="post" action="{{ path('app_periode_delete', {'id': periode.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ periode.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
