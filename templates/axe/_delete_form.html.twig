<form method="post" action="{{ path('app_axe_delete', {'id': activite.id}) }}" onsubmit="return confirm('Etes-vous sure de vouloir supprimer cet axe ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('Supprimer' ~ activite.id) }}">
    <button class="btn">Supprimer</button>
</form>
