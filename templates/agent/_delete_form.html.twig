<form method="post" action="{{ path('app_agent_delete', {'identifiant_agent': agent.identifiantAgent}) }}" onsubmit="return confirm('Etes-vous sure de vouloir suprimer cet agent?');">
    <input type="hidden" name="_token" value="{{ csrf_token('Supprimer' ~ agent.identifiantAgent) }}">
    <button class="btn">Supprimer</button>
</form>
