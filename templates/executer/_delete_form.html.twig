<form method="post" action="{{ path('app_executer_delete', {'id': executer.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ executer.id) }}">
    <button class="btn">Delete</button>
</form>
