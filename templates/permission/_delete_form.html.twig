<form method="post" action="{{ path('app_permission_delete', {'id': permission.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ permission.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
