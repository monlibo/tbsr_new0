
<form class="d-inline-block" method="post" action="{{ path('app_bailleur_delete', {'id': bailleur.id}) }}"
      onsubmit="return confirm('Voulez-vous vraiment supprimer cet enregistrement?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bailleur.id) }}">
{#    <button class="btn btn-xs text-red "><i class="fa fa-trash"></i></button>#}
    <button class="btn btn-danger btn-xs text-red "><i class="fa fa-trash"></i></button>
</form>
