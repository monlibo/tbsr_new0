{% extends 'base.html.twig' %}

{% block title %}TypeDonnee{% endblock %}

{% block body %}
    <h1>Type de Donnee</h1>
    {% for message in app.flashes('success') %}
        <div class="alert alert-success">
            {{ message }}
        </div>
    {% endfor %}
    <table class="table">
        <tbody>
   
            <tr>
                <th>Code</th>
                <td>{{ typeDonnee.code }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ typeDonnee.libelle }}</td>
            </tr>

        </tbody>
    </table>

    <a href="{{ path('app_typeDonnee_index') }}">Retour à la liste</a>

    <a href="{{ path('app_typeDonnee_edit', {'id': typeDonnee.id}) }}">Modifier</a>
    </br>
    {{ include('typeDonnee/_delete_form.html.twig') }}
{% endblock %}
