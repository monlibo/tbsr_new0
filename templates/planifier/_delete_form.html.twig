<form method="post" action="{{ path('app_planifier_delete', {'id': planifier.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ planifier.id) }}">
    <button class="btn">Delete</button>
</form>
