{% extends 'base.html.twig' %}

{% block title %}Devise{% endblock %}

{% block body %}
<div class="container">

    <h1>Modifier une Devise</h1>

    {{ include('devise/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a href="{{ path('app_devise_index') }}">Retour à la liste</a>

    {{ include('devise/_delete_form.html.twig') }}
</div>
{% endblock %}
